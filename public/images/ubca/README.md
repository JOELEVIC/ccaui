# UBCA / Academy images (legacy)

Academy images are now **imported directly** from `src/assets/images/ubca/` so they are bundled and always deployed. This folder in `public/` is no longer used by the app; you can remove it or keep it as a backup. See `src/assets/images/ubca/index.ts`.
